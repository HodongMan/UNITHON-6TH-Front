import * as React from 'react';

export default class TimelineHeader extends React.PureComponent<{}, {}> {

    render() {
        return (
            <h1 className="project-name">RESPONSIVE TIMELINE</h1>
        );
    }
}